<h1>Nos douches</h1>
<hr>
<div class="card-deck">
    {% for shower in productShower %}
        <div class="card">
            <img class="card-img-top" src="{{ shower.showerUrlImage }}" alt="Card image cap">
            <div class="card-body">
                <a href="{{ path('productShower.show', {slug: shower.slug, id: shower.id}) }}">
                    <h6 class="card-title">{{ shower.showerName }} </h6></a>
                <p class="card-text">{{ shower.showerPrice }} €</p>
            </div>
        </div>
    {% endfor %}     
</div>